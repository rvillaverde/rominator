# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
development:
  adapter: postgresql
  reconnect: false
  database: rominator_development
  pool: 5
  timeout: 5000
  host: localhost
  encoding: utf8
  username: rominator
  password: rominator

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  adapter: postgresql
  reconnect: false
  database: rominator_test
  pool: 5
  timeout: 5000
  host: localhost
  encoding: utf8
  username: rominator
  password: rominator

production:
  adapter: postgresql
  reconnect: false
  database: rominator_production
  pool: 5
  timeout: 5000
  host: localhost
  encoding: utf8
  username: rominator
  password: rominator
